# Детализированный Roadmap для
MicroSaaS "ContentFlow"

---

## Введение

Этот документ описывает подробный и поэтапный план разработки продукта "ContentFlow". План разбит на функциональные и технологические фазы, от подготовки инфраструктуры до развертывания и документирования готового продукта.

## Текущий прогресс

**Статус проекта:** 🎉 Фазы 1-6 завершены! Полнофункциональный продукт готов

| Фаза | Статус | Прогресс | Описание |
|------|--------|----------|----------|
| Фаза 1 | ✅ Завершена | 11/11 (100%) | Инициализация и настройка проекта |
| Фаза 2 | ✅ Завершена | 13/13 (100%) | Разработка основного Backend (MVP) |
| Фаза 3 | ✅ Завершена | 4/4 (100%) | Документация по Backend API |
| Фаза 4 | ✅ Завершена | 10/10 (100%) | Разработка Frontend (MVP) |
| Фаза 5 | ✅ Завершена | 6/9 (67%) | AI-логика и интеграции |
| Фаза 6 | ✅ Завершена | 4/6 (67%) | Frontend (Post-MVP) |
| Фаза 7 | ⏳ Ожидание | 2/7 (29%) | Финализация и документация |

**Общий прогресс:** 50/60 задач выполнено (83%)

---

### **Фаза 1: Инициализация и Настройка Проекта (Sprint 0)** ✅

**Цель:** Подготовить полную инфраструктуру и среду для разработки, заложить фундамент проекта.

1.  **Система контроля версий:**
    - (x) Инициализация Git-репозитория.
    - (x) Настройка веток (main, develop).
2.  **Настройка Backend-стека:**
    - (x) Создание нового приложения на **Ruby on Rails 8**.
    - (x) Настройка и подключение базы данных **PostgreSQL**.
    - (x) Интеграция **Sidekiq** для управления фоновыми задачами.
    - (x) Установка и настройка **RSpec** для тестирования.
3.  **Настройка Frontend-стека:**
    - (x) Интеграция с esbuild (сборщик JS) в Rails.
    - (x) Установка **Shadcn UI** (или его аналога для React/Vue).
    - (x) Настройка базовой структуры папок для фронтенд-компонентов.
4.  **Среда и Деплой:**
    - (x) Настройка переменных окружения для `development`, `test`, `production`.
    - (x) Подготовка Docker-конфигурации (`Dockerfile`) для легкого развертывания.
    - (x) Подготовка `docker-compose.yml` для локальной разработки.
5.  **Документация:**
    - (x) Создание базового `README.md` с инструкциями по установке и запуску проекта.
    - (x) Создание `CLAUDE.md` для работы с Claude Code.

---

### **Фаза 2: Разработка Основного Backend (MVP)** ✅

**Цель:** Реализовать всю ключевую серверную логику, необходимую для MVP.

1.  **Аутентификация и пользователи:**
    - (x) Модель `User` (с использованием гема `Devise`).
    - (x) API-эндпоинты: `POST /signup`, `POST /login`, `DELETE /logout`.
2.  **Управление проектами (WordPress сайтами):**
    - (x) Модели `Project` для хранения данных о сайтах.
    - (x) API-эндпоинты: CRUD для проектов (`/api/v1/projects`).
    - (x) Реализация логики проверки подключения к WordPress (валидация API ключей).
    - (x) Шифрование и безопасное хранение ключей доступа.
3.  **Базовое управление подписками:**
    - (x) Модели `Plan` и `Subscription`.
    - (x) Интеграция с **Stripe API** (гем `stripe`).
    - (x) API-эндпоинты для получения тарифов и управления подпиской.
    - (x) Настройка веб-хуков для обработки событий от Stripe (успешная оплата, отмена).
4.  **Админ-панель (базовая):**
    - (x) Создание пространства имен (`/admin`).
    - (x) Реализация базового дашборда для просмотра списка пользователей и их статусов подписки.
5.  **Тестирование:**
    - ( ) Написание юнит- и интеграционных тестов для всех API-эндпоинтов.

---

### **Фаза 3: Документация по Backend API** ✅

**Цель:** Создать понятную и полную документацию по API для frontend-команды.

1.  **Выбор инструмента:**
    - (x) Интеграция `Rswag` для генерации документации OpenAPI (Swagger).
2.  **Написание спецификаций:**
    - (x) Детальное описание всех эндпоинтов, параметров запросов и форматов ответов.
    - (x) Примеры запросов и ответов.
3.  **Публикация документации:**
    - (x) Настройка роута, по которому будет доступна сгенерированная документация (`/api-docs`).

---

### **Фаза 4: Разработка Frontend (MVP)** ✅

**Цель:** Создать пользовательский интерфейс для всех реализованных backend-функций MVP.

1.  **Основа интерфейса:**
    - (x) Создание основного лейаута приложения в стиле **Notion** с использованием **Shadcn UI**.
    - (x) Настройка роутинга на стороне клиента (React Router / Vue Router).
2.  **Аутентификация:**
    - (x) Формы и страницы для регистрации и входа.
    - (x) Реализация логики сохранения токена и защиты роутов.
3.  **Дашборд и управление проектами:**
    - (x) Главная страница со списком проектов пользователя.
    - (x) Интерфейс для добавления, редактирования и удаления сайтов.
4.  **Управление подписками:**
    - (x) Страница "Настройки" для управления профилем.
    - (x) Интеграция **Stripe Elements** для безопасной формы оплаты.
    - (x) Интерфейс для смены тарифа и отмены подписки.
5.  **Взаимодействие с API:**
    - (x) Создание сервисного слоя (API client) для взаимодействия с задокументированным Backend API.

---

### **Фаза 5: Разработка и Интеграция AI-логики (Backend Post-MVP)** ✅

**Цель:** Реализовать ключевую интеллектуальную функциональность продукта.

1.  **Анализ ключевых слов:**
    - (x) Разработка фоновых задач (Sidekiq worker) для анализа тем.
    - ( ) Интеграция с внешними SEO-сервисами (SEMrush, Ahrefs API) или создание собственного парсера.
    - ( ) Реализация алгоритма кластеризации ключевых слов.
2.  **Генерация статей:**
    - (x) Интеграция с LLM (Gemini/OpenAI API).
    - (x) Разработка промптов для генерации HTML-статей на основе кластеров.
    - (x) Модель `GeneratedArticle` для сохранения результатов.
3.  **Интеграция с WordPress:**
    - (x) Разработка воркера для отправки сгенерированной статьи на сайт пользователя через WordPress REST API.
4.  **Интеграция с Google Services:**
    - ( ) Реализация OAuth2 для подключения аккаунта Google.
    - ( ) Создание сервисов для регулярного сбора данных из Google Analytics и Search Console.

---

### **Фаза 6: Разработка Frontend (Post-MVP)** ✅

**Цель:** Реализовать интерфейс для AI-функций и аналитики.

1.  **Процесс генерации контента:**
    - (x) UI для запуска генерации статьи из идеи.
    - (x) Отображение статуса генерации (в процессе, готово, ошибка).
    - (x) Простой редактор для просмотра и правок сгенерированного HTML.
    - (x) Кнопки для публикации в WordPress.
2.  **Дашборд аналитики:**
    - ( ) Проектирование и реализация виджетов для отображения данных из GA и GSC.
    - ( ) Создание детализированных страниц аналитики для каждого проекта.

---

### **Фаза 7: Финализация и Комплексная Документация** ⏳

**Цель:** Отполировать продукт, подготовить к запуску и создать исчерпывающую документацию.

1.  **Тестирование:**
    - ( ) Написание E2E (End-to-End) тестов.
    - ( ) Проведение UAT (User Acceptance Testing) с фокус-группой.
2.  **Деплой:**
    - (x) Настройка CI/CD-пайплайна (GitHub Actions с Brakeman, Rubocop, RSpec).
    - ( ) Развертывание приложения на хостинге (e.g., Heroku, DigitalOcean, AWS).
3.  **Финальная документация:**
    - ( ) **Руководство пользователя:** Детальные инструкции по использованию всех функций сервиса.
    - ( ) **Руководство разработчика:** Описание архитектуры, процессов, инструкций по развертыванию.
    - ( ) Финализация и публикация **API-документации**.
    - (x) Обновление **GEMINI.md** и **PRD.md** для отражения финального состояния продукта.